[build]
  builder = "nixpacks"
  buildCommand = "./gradlew clean build -x test"

[deploy]
  startCommand = "java -jar build/libs/agro-trace-backend-0.0.1-SNAPSHOT.jar"
  healthcheckPath = "/api/v1/actuator/health"
  restartPolicyType = "ON_FAILURE"

[service]
  name = "agro-trace-backend"
